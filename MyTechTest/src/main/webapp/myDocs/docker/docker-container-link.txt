传统容器链接（Legacy container links）
docker链接允许容器发现彼此并安全的传输信息。
警告：--link标志是一个过期的标志，未来可能被删除。除非你绝对需要继续使用它，建议你使用用户定义的网络来帮助容器之间的通讯。
用户定义的网络有一个特性不支持而--link支持的特性是在容器之间共享环境变量。然而，你可以使用其它的机制例如卷来共享环境变量，这是一个更可控的方式。

使用网络端口映射连接
